/* ============================================================ */
/* إعداد المزامنة السحابية بين الأجهزة (اختياري)                */
/* ============================================================ */
/* اتركه فاضياً كما هو إذا ما تحتاج مزامنة — المنصة تعمل بشكل طبيعي محلياً فقط.
   لتفعيل المزامنة بين الأجهزة (الكمبيوتر والجوال معاً):
   1) أنشئ مشروع Firebase مجاني من https://console.firebase.google.com
   2) فعّل "Firestore Database" من القائمة الجانبية (وضع الإنتاج أو التجربة).
   3) من Project settings → عام → "أضف تطبيق ويب"، انسخ بيانات firebaseConfig.
   4) الصق القيم في الأسفل بدل الفراغات، واحفظ الملف.
   ملاحظة: الشواهد (الصور والفيديو) لا تُزامَن بين الأجهزة — تبقى محفوظة
   محلياً في كل جهاز فقط، بسبب حجمها الكبير. باقي البيانات (المهام، حالة
   الإنجاز، المسابقات، الطلاب، الخطة، الجدول) تتزامن تلقائياً وفورياً. */
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
